#include <bits/stdc++.h>
using namespace std;

typedef long long int ll;
int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  ll x, y;
  cin >> x >> y;
  ll sum = ((x * 5000) + (y * 9800));
  cout << sum << endl;
  return 0;
}